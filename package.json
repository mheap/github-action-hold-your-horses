{
  "name": "github-action-hold-your-horses",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "jest",
    "build": "npx @vercel/ncc build && npx convert-action"
  },
  "dependencies": {
    "actions-toolkit": "^3.0.1",
    "iso8601-duration": "^1.2.0"
  },
  "devDependencies": {
    "jest": "^25.1.0",
    "mockdate": "^2.0.5",
    "mocked-env": "^1.3.2",
    "nock": "^12.0.2"
  }
}
